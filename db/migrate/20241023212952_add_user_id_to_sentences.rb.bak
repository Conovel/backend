# frozen_string_literal: true

# Sentencesテーブルにuser_idカラムを追加するマイグレーションファイル
class AddUserIdToSentences < ActiveRecord::Migration[7.0]
  def change
    add_column :sentences, :user_id, :bigint, null: false
    add_foreign_key :sentences, :users, column: :user_id, primary_key: :user_id
    add_index :sentences, :user_id
  end
end
